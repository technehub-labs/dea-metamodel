{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://technehub-labs.github.io/dea-metamodel/schemas/entities/business-service.json",
  "metamodel_version": "0.7.0",
  "title": "BusinessService",
  "description": "A service exposed to the business or to external consumers.",
  "type": "object",
  "required": [
    "id",
    "name",
    "type",
    "version",
    "service_type"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*:[a-z0-9-]+$",
      "examples": [
        "dea:service-customer-onboarding"
      ]
    },
    "type": {
      "type": "string",
      "const": "BusinessService"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "description": {
      "type": "string",
      "maxLength": 2000
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "service_type": {
      "type": "string",
      "enum": [
        "internal",
        "external",
        "partner",
        "public"
      ],
      "description": "Service exposure scope."
    },
    "owner": {
      "type": "string",
      "deprecated": true,
      "description": "DEPRECATED (CR-002, CR-2F): relationship state is authoritative in the canonical relationship registry (metamodel/registry/relationships.yaml), not in entity schemas. This convenience property will be physically removed in CR-003."
    },
    "provided_by": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "SolutionComponent IDs that provide this service. DEPRECATED (CR-002, CR-2F): relationship state is authoritative in the canonical relationship registry (metamodel/registry/relationships.yaml), not in entity schemas. This convenience property will be physically removed in CR-003.",
      "deprecated": true
    },
    "consumed_by": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "SolutionComponent or Process IDs that consume this service. DEPRECATED (CR-002, CR-2F): relationship state is authoritative in the canonical relationship registry (metamodel/registry/relationships.yaml), not in entity schemas. This convenience property will be physically removed in CR-003.",
      "deprecated": true
    },
    "sla": {
      "type": "object",
      "properties": {
        "availability": {
          "type": "string"
        },
        "latency_p99_ms": {
          "type": "integer"
        },
        "throughput_rps": {
          "type": "integer"
        }
      }
    },
    "relationships": {
      "type": "array",
      "items": {
        "$ref": "../relationships/relationship-instance.json"
      }
    },
    "metadata": {
      "$ref": "entity.json#/properties/metadata"
    }
  }
}
