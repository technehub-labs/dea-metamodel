{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://technehub-labs.github.io/dea-metamodel/schemas/entities/regulation.json",
  "metamodel_version": "0.7.0",
  "title": "Regulation",
  "description": "An externally imposed obligation (law, industry standard with force, contractual mandate) the enterprise must comply with. OpenDEAM v0.3.0 (ADR-0003), L2/Risk & Compliance.",
  "type": "object",
  "required": [
    "id",
    "name",
    "type",
    "version"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*:[a-z0-9-]+$",
      "examples": [
        "dea:regulation-example"
      ]
    },
    "type": {
      "type": "string",
      "const": "Regulation"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "description": {
      "type": "string",
      "maxLength": 2000
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "jurisdiction": {
      "type": "string",
      "description": "Jurisdiction or authority imposing the obligation."
    },
    "mandates": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Control ids required by this regulation. DEPRECATED (CR-002, CR-2F): relationship state is authoritative in the canonical relationship registry (metamodel/registry/relationships.yaml), not in entity schemas. This convenience property will be physically removed in CR-003.",
      "deprecated": true
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "relationships": {
      "type": "array",
      "items": {
        "$ref": "../relationships/relationship-instance.json"
      }
    },
    "metadata": {
      "$ref": "entity.json#/properties/metadata"
    }
  }
}
