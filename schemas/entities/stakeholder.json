{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://technehub-labs.github.io/dea-metamodel/schemas/entities/stakeholder.json",
  "title": "Stakeholder",
  "description": "An external or affected party whose relationship with the enterprise is engaged in or affected by enterprise processes. Stakeholders are NOT internal performers — internal performers (employees, teams, systems, AI agents) are Actors, not Stakeholders. Catalog: technehub-labs/dea-catalog-stakeholders.",
  "type": "object",
  "required": ["id", "name", "type", "version", "stakeholder_type"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*:[a-z0-9-]+$",
      "examples": ["dea:stakeholder-acme-corp"]
    },
    "type": { "type": "string", "const": "Stakeholder" },
    "name": { "type": "string", "minLength": 1, "maxLength": 200 },
    "description": { "type": "string", "maxLength": 2000 },
    "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
    "stakeholder_type": {
      "type": "string",
      "enum": [
        "customer",
        "partner",
        "supplier",
        "regulator",
        "investor",
        "community",
        "board"
      ],
      "description": "The relationship class the stakeholder holds with the enterprise. Open set — new stakeholder types (e.g. AI agents, DAO treasuries) can be added without schema changes when enterprise relationships genuinely require them."
    },
    "relationship_direction": {
      "type": "string",
      "enum": ["inbound", "outbound", "bidirectional", "governance"],
      "description": "How value flows between the stakeholder and the enterprise: inbound (enterprise receives value), outbound (enterprise delivers value), bidirectional (co-created/shared), governance (accountability/compliance)."
    },
    "primary_contact": { "type": "string" },
    "external_identifiers": {
      "type": "object",
      "description": "External IDs — registration numbers, LEI, DUNS, ticker, etc. Free-form key/value.",
      "additionalProperties": { "type": "string" }
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" }
    },
    "relationships": {
      "type": "array",
      "items": { "$ref": "../relationships/relationship-instance.json" }
    },
    "metadata": { "$ref": "entity.json#/properties/metadata" }
  }
}