{
  "name": "@dea-metamodel/typescript",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript interfaces auto-generated from DEA metamodel JSON schemas (schemas/entities/*.json). Type-checked via `tsc --noEmit` in CI.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "echo 'Generator not yet implemented — schemas/entities/*.json -> src/interfaces.ts' && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}