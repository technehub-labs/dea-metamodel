# Terminology Registry — POINTER (downgraded after CR-CM-001)
#
# This file is no longer the terminology registry. The canonical home is:
#
#   technehub-labs/dea-concepts-model · governance/terminology-registry.yaml
#
# designated by CR-CM-000A §14 and realised by CR-CM-001 (2026-08-25),
# with the governed term shape (name/namespace/status/owner) preserved
# byte-exactly at version 1.1.0.
#
# This pointer exists so existing references resolve to a governed
# statement of where the authority now lives. Do NOT add registry content
# here — the conformance suite
# (tests/conformance/test_015_terminology_registry.py) fails if any
# content block reappears (drift guard).
#
# Source of authority: change-requests/CR-CM-000.md (this repo),
# change-requests/CR-CM-001.md (dea-concepts-model).

registry:
  id: dea:terminology-registry
  version: 1.1.0
  status: pointer
  introduced_by: CR-CM-000
  extended_by: CR-CM-000A
  homed_by: CR-CM-001
  purpose: >-
    Establish semantic boundaries between the Enterprise Concept Framework,
    the OpenDEA Concepts Model, the OpenDEA Foundational Metamodel, the
    Catalogs, and the Profiles.
  canonical_home:
    repository: technehub-labs/dea-concepts-model
    path: governance/terminology-registry.yaml
    version: 1.1.0
  migration: >-
    Content migrated byte-exactly to the canonical home by CR-CM-001.
    This copy — the canonical interim registry under CR-CM-000A §14 until
    the Concepts Model repository existed — is downgraded to a pointer.
    All registry governance (terms, conceptual relationship verbs,
    prohibited semantics, initial Concept Areas, artifact boundaries, and
    acceptance-criterion rules) is maintained in the canonical home only.
